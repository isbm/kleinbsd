# rpi4

Placeholder for a Raspberry Pi 4 image profile.

The project is now structured so this profile can define its own machine, kernel config, image build path, firmware handling, and run/deploy workflow without changing the QEMU amd64 profile.
